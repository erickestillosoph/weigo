export type Payment = {
    message: string;
    status: string;
    code: string;
};
export type CreditCard = {
    message: string;
    status: string;
    code: string;
};
export type ServiceModel = {
    message: string;
    status: string;
    code: string;
};
export type FilteredPayments = {
    message: string;
    status: string;
    code: string;
};
export type PreSelectingPayments = {
    message: string;
    status: string;
    code: string;
};

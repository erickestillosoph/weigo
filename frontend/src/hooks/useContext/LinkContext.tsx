// import React, { useContext, ReactNode } from "react";

// // Create a context
// const useLinkContext = React.createContext({});

// // Create a component that uses the context
// const LinkContext = () => {
//     const basename = useContext(useLinkContext);
//     // rest of the component
//     return <div>{basename as ReactNode}</div>;
// };

// export default LinkContext;
